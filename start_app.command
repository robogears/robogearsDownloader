#!/bin/bash
# robogears Downloader — macOS dev launcher.
# Double-click from Finder, or run `./start_app.command` from a terminal.
# (macOS equivalent of start_app.bat.)
cd "$(dirname "$0")"
npm start
